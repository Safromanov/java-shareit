CREATE TABLE IF NOT EXISTS users
(
    user_id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    name    VARCHAR(255),
    email   VARCHAR(255),
    CONSTRAINT pk_users PRIMARY KEY (user_id)
);

CREATE TABLE IF NOT EXISTS items
(
    id          BIGINT AUTO_INCREMENT NOT NULL,
    name        VARCHAR(255),
    description VARCHAR(255),
    available   BOOLEAN               NOT NULL,
    user_id     BIGINT,
    request_id  BIGINT,
    CONSTRAINT pk_items PRIMARY KEY (id)
);
CREATE TABLE IF NOT EXISTS item_request
(
    id           BIGINT AUTO_INCREMENT NOT NULL,
    description  VARCHAR(255),
    requestor_id BIGINT,
    created      TIMESTAMP,
    CONSTRAINT pk_itemrequest PRIMARY KEY (id)
);


CREATE TABLE IF NOT EXISTS booking
(
    id             BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    start          TIMESTAMP,
    end_booking    TIMESTAMP,
    item_id        BIGINT,
    booker_user_id BIGINT,
    status         VARCHAR(255),
    CONSTRAINT pk_booking PRIMARY KEY (id)
);

ALTER TABLE items
    ADD CONSTRAINT IF NOT EXISTS fk_items_on_request FOREIGN KEY (request_id) REFERENCES item_request (id);

ALTER TABLE items
    ADD CONSTRAINT IF NOT EXISTS fk_items_on_user FOREIGN KEY (user_id) REFERENCES users (user_id);


ALTER TABLE item_request
    ADD CONSTRAINT IF NOT EXISTS fk_itemrequest_on_requestor FOREIGN KEY (requestor_id) REFERENCES users (user_id);



ALTER TABLE booking
    ADD CONSTRAINT IF NOT EXISTS fk_booking_on_booker_user FOREIGN KEY (booker_user_id) REFERENCES users (user_id);

ALTER TABLE booking
    ADD CONSTRAINT IF NOT EXISTS fk_booking_on_item FOREIGN KEY (item_id) REFERENCES items (id);